---
title: "Untitled"
author: "Timothy Wong"
date: "29/10/2021"
output: html_document
---

```{r}
library(devtools)
library(tidyverse)
library(fs)


##################################################################
##                 Package creation shenanigans                 ##
##                           (boring)                           ##
##################################################################

# create_package("M:/Anxiety/12. Master Database Project/Tim W/August files/bmemTOOLS")
  # Creates a barebone package at the specified directory
# use_git()
  # Starts a git repo
# use_r(func)
  # Creates a new script named "func.R" under the /R directory
# load_all()
  # Use inside package directory. Simulates building, installing and attaching the package
  # Also available through Build > Load all or "Ctrl + Shift+ L"
# check()
  # Check this often, GOLD STANDARD for checking R package is in full working order
  # Also available through Build > Check or "Ctrl + Shift + E"
# DESCRIPTION: provide basic info about pkg
# use_license_mit("name")
  # Writes a license file


##################################################################
##                          Man-making                          ##
##################################################################

# ROXYGEN2 SKELETON: Code > Insert Roxygen Skeleton or "Ctrl + Alt + Shift + R"
# document()
  # Turns roxygen comments into latex-formatted man docs
  # For each roxygen-formatted script with @export, updates the namespace with export(func)
  # Also available through Build > Document or through "Ctrl + Shift + D"


#################################################################
##                        Installation!                        ##
#################################################################

# install()
  # Installs the minimum viable product
  # Also available through Build > Install and Restart or "Ctrl + Shift + B"
# library(func): from this point, can be treated like any other package


#################################################################
##                    Testing, testing, 1-2                    ##
#################################################################

# library(testthat)
# use_testthat()
  # Creates a test directory and adds testthat to Suggests in DESCRIPTION
  # Still have to create tests
# use_test(func)
  # 1: Specify the basename of the function, creates 'test-basename.R'
  # 2: Use while editing the function script, creates 'test-basename.R'
# devtools::test()
  # Runs all tests defined in /tests/testthat
  # Available through Build > Test package or "Ctrl + Shift + T"


#################################################################
##             Imports: on the shoulders of giants             ##
#################################################################

# use_package(pkg)
  # Declares "pkg" under Imports in the DESCRPTION file



```
